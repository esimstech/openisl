if(${OPENISL_FMI})
    add_subdirectory("fmi")
endif()
if(${OPENISL_LUA})
    add_subdirectory("lua")
endif()
if(${OPENISL_MODELICA})
    add_subdirectory("modelica")
endif()
if(${OPENISL_OMNETPP})
    add_subdirectory("omnetpp")
endif()

add_subdirectory("isl_utils")
add_subdirectory("isl_api")
if(MSVC)
    add_subdirectory("isl_api_cli")
endif()
if(${OPENISL_JAVA_API})
    add_subdirectory("isl_api_java")
endif()
if(${OPENISL_PYTHON_API})
    add_subdirectory("isl_api_python")
endif()
